{
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "call scripts\\install-backend.bat"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "call scripts\\run-backend.bat"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "call scripts\\test-backend.bat"
      }
    }
  }
}