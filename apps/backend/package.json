{
  "name": "@mono-repo-skeleton/backend",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "call scripts\\install-backend.bat"
        }
      },
      "serve": {
        "executor": "nx:run-commands",
        "options": {
          "command": "call scripts\\run-backend.bat"
        }
      }
    }
  }
}
